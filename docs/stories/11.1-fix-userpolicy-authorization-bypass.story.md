---
id: 11.1-fix-userpolicy-authorization-bypass
slug: fix-userpolicy-authorization-bypass
title: "Fix: UserPolicy bypassa completamente l'autorizzazione (account takeover)"
description: "UserPolicy::view/create/update/delete ritornano sempre true — qualunque utente autenticato puo' vedere/modificare/cancellare qualunque altro utente, admin inclusi."
document_type: story
category: security
scope: authorization
status: backlog
version: "1.0"
language: it
ecosystem: laravel
priority: critical
created_at: 2026-09-22
updated_at: 2026-09-22
tags: [security, policy, authorization, account-takeover, critical]
related:
  - Modules/User/docs/bmad/perfection-brainstorming.md
  - Modules/User/docs/bmad/perfection-architecture.md
  - Modules/User/docs/bmad/perfection-decision-log.md
  - Modules/User/docs/stories/11.4-encrypt-socialite-token.story.md
github:
  repository: null
  issues: []
  note: "Non pubblicato come issue pubblico — repo laraxot/module_user_fila5 e' PUBLIC, vedi perfection-decision-log.md sezione disclosure. Valutare GitHub Security Advisory privata."
---

# Story 11.1: fix UserPolicy authorization bypass

## Contesto (audit BMAD "perfezione assoluta", 2026-09-22)

`laravel/Modules/User/app/Models/Policies/UserPolicy.php` estende
`UserBasePolicy` (pass-through vuoto verso `XotBasePolicy`), non
`BaseUserPolicy.php` (classe sorella quasi omonima, gia' scritta correttamente con
controllo ruolo `hasRole(['super-admin','admin','hr-manager'])` ma mai estesa da
nessuno — `grep -rln "extends BaseUserPolicy"` → 0 risultati).

```php
class UserPolicy extends UserBasePolicy
{
    public function view(Post $_user, Post $_post): bool   { return true; }
    public function create(Post $_user): bool               { return true; }
    public function update(Post $_user, Post $_post): bool   { return true; }
    public function delete(Post $_user, Post $_post): bool   { return true; }
}
```

Laravel auto-scopre questa policy per convenzione (`{namespace}\Policies\
{Model}Policy`) — nessuna registrazione esplicita necessaria/presente. `UserResource`
Filament non ha override di autorizzazione proprio, quindi eredita questo
comportamento.

**Impatto verificato:** qualunque utente autenticato che ottenga lo UUID di un
altro utente puo', navigando a `/…/users/{uuid}/edit`: vederne il profilo,
cambiargli email/password (account takeover), cancellarne l'account — admin
inclusi, nessun self-check.

## User story

Come utente autenticato senza privilegi elevati,
non devo poter vedere, modificare o cancellare il profilo di un altro utente,
cosi' che l'identity module protegga davvero gli account.

## Acceptance criteria

- [ ] `UserPolicy` estende una base a controllo ruolo reale (non piu'
      `UserBasePolicy` pass-through) — implementa ADR-P001 in
      `perfection-architecture.md`: le due classi `UserBasePolicy`/
      `BaseUserPolicy` sono rinominate per rompere la naming collision (nomi
      finali a discrezione dell'implementazione, vincolo: non devono differire
      solo per l'ordine di due parole).
- [ ] `view()`, `update()`, `delete()` negano l'accesso a un utente non
      autorizzato verso il profilo di un altro utente (self o ruolo admin/
      super-admin/hr-manager, coerente con la logica gia' scritta in
      `BaseUserPolicy` esistente).
- [ ] `create()` resta autorizzato solo a ruoli con permesso di creazione utenti
      (non un `true` incondizionato).
- [ ] Nessuna classe (`BaseUserPolicy`/base rinominata) resta dead code dopo il
      fix — verificare `grep -rln "extends <nome-nuovo>"` trova almeno
      `UserPolicy`.
- [ ] Test comportamentale reale (vedi story 14.1, dipendente da questa) prova
      sia il caso autorizzato sia quello negato.
- [ ] PHPStan level 10 zero errori sul file modificato.
- [ ] Nessuna migration/scrittura DB richiesta da questo fix (e' solo codice
      applicativo).

## Piano di verifica

1. `php artisan tinker` (read-only): istanziare due utenti non-admin distinti,
   verificare `Gate::forUser($u1)->denies('update', $u2)` prima del fix (riproduce
   il bug) e dopo il fix (`denies` → true).
2. `Gate::forUser($admin)->allows('update', $qualsiasiUtente)` resta `true` dopo
   il fix, per non rompere il workflow amministrativo esistente.
3. Rilanciare `php -d memory_limit=2048M ./vendor/bin/phpstan analyse
   --memory-limit=-1 --no-progress Modules/User` → 0 errori.

## Rischi

- Rompere il flusso di self-edit (un utente deve poter modificare il *proprio*
  profilo) — la logica in `BaseUserPolicy` esistente va verificata/estesa per
  coprire esplicitamente il caso self, non solo i ruoli elevati.
- `UserResource` Filament potrebbe avere altri punti che assumono
  l'autorizzazione sempre permessa (bulk actions, relation manager) — verificare
  in fase di implementazione, non solo la Policy isolata.

## Riferimenti

- [perfection-brainstorming.md §1.1](../bmad/perfection-brainstorming.md)
- [perfection-architecture.md ADR-P001](../bmad/perfection-architecture.md)
- [perfection-decision-log.md — disclosure](../bmad/perfection-decision-log.md)
