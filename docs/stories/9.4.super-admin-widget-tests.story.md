---
id: "9.4"
epic: 9
story: "4"
title: "Test Pest SuperAdmin widget"
slug: super-admin-widget-tests
status: done
scope: module:User
epic_title: 9 — SuperAdmin user-menu widget
github:
  repository: https://github.com/laraxot/module_user_fila5
  issues: https://github.com/laraxot/module_user_fila5/issues
  discussions: https://github.com/laraxot/module_user_fila5/discussions
  issue_status: "da aprire — gh non autenticato il 2026-09-21"
related:
  - ../bmad/prd.md
  - ../bmad/ux-design.md
  - ./9.2.admin-panel-provider-hook.story.md
---

# 9.4: Test Pest SuperAdmin widget

**Story ID:** 9.4  
**Epic:** 9 — SuperAdmin user-menu widget  
**Slug:** super-admin-widget-tests  
**Status:** ready-for-dev

## GitHub (tracciamento)

| Risorsa | Ruolo |
|---------|-------|
| DA CREARE | issue `laraxot/module_user_fila5` |
| DA CREARE | discussion |

```bash
gh -R laraxot/module_user_fila5 issue create --title "9.4 Pest SuperAdminWidget" --body "Canon: laravel/Modules/User/docs/stories/9.4.super-admin-widget-tests.story.md"
```

## Story

Come **maintainer**,
voglio **test che fissano visibilità, toggle e assenza in dashboard**,
così che **un futuro hook/discovery non rompa `/admin` in silenzio**.

## Acceptance Criteria

1. Test: utente `super-admin` autenticato sul panel vede l’icona (o il widget renderizza il bottone non ruotato). [Source: prd.md#FR-001] [Source: ux-design.md]
2. Test: utente `negate-super-admin` vede lo stato ruotato. [Source: ux-design.md]
3. Test: utente senza quei ruoli non vede i bottoni. [Source: prd.md#FR-001]
4. Test: invocare `toggleSuperAdmin` sul widget inverte `hasRole('super-admin')` / `hasRole('negate-super-admin')`. [Source: prd.md#FR-002]
5. Test: `SuperAdminWidget::isDiscovered()` è false. [Source: prd.md#FR-006]
6. I test vivono sotto `Modules/User/tests/` con suffisso `Test.php`. Nessun `assertTrue(true)`. [Source: project-context.md]

## Tasks / Subtasks

- [x] Feature o Livewire test visibilità tre stati (AC: #1, #2, #3)
- [x] Test toggle ruoli sul profilo (AC: #4)
- [x] Unit `isDiscovered` (AC: #5) — già presente da 9.1, verificato invariato
- [x] Naming `*Test.php` (AC: #6)

## Dev Notes

- Pattern esistenti: `tests/Feature/Filament/Widgets/LoginWidgetTest.php`, `tests/Unit/Filament/Widgets/LogoutWidgetTest.php`. [Inference dal tree User]
- Non usare `RefreshDatabase` se la religione dati sacri del repo lo vieta: preferire DB `*_test` già previsto. [Source: regole progetto]
- Non mockare `toggleSuperAdmin` togliendo Spatie: il valore è l’inversione reale dei ruoli. [Source: architecture.md#adr-002]
- Pest può non girare se `.env.testing` manca: la story chiede i file test, non l’esecuzione in planning. [Source: bmad-epics testing = strategy]

## Testing

- **Approach:** questa story *è* i test. Planning: non eseguirli qui.
- **Per-AC:** un test (o dataset) per AC 1–5.
- **Fixtures:** user+profile con ruoli Spatie; panel admin.
- **Edge:** profilo senza user → trait lancia; non è obbligatorio coprire se già coperto altrove.

## Dependency Maps

- **Blocked by:** 9.2 (hook attivo). 9.1 necessaria. 9.3 opzionale in parallelo.
- **Blocks:** none
- **External:** suite Pest User, DB test se feature

## Owned File/Module Scope

- `laravel/Modules/User/tests/Feature/Filament/Widgets/SuperAdminWidgetTest.php` — nuovo
- `laravel/Modules/User/tests/Unit/Filament/Widgets/SuperAdminWidgetTest.php` — nuovo (isDiscovered)
- Shared/contended: **non** provider, **non** widget class (read-only)

## Learnings from Previous Stories

- LoginWidgetTest è il riferimento per “widget Filament + auth”.

## Dev Agent Record

**Status finale:** done. Tutti e 6 gli AC verificati con prove reali (comandi eseguiti, non a memoria).

**File toccati (Owned File/Module Scope):**
- `Modules/User/tests/Feature/Filament/Widgets/SuperAdminWidgetTest.php` — **nuovo**. 7 test: 3 di visibilità (AC#1-3), 2 di toggle ruoli (AC#4), 1 di redirect 303 reale (richiesta esplicita del task, distinta da AC#4), 1 di assenza dalla dashboard (`Filament::getPanel('user::admin')->getWidgets()`, verifica indipendente di AC#5 lato panel/discovery invece che lato classe).
- `Modules/User/tests/Unit/Filament/Widgets/SuperAdminWidgetTest.php` — **non modificato**. Già creato nel commit `2c83c20c8` della story 9.1: copre già AC#5 (`isDiscovered()` false) e AC#6 (naming, nessun `assertTrue(true)`). Verificato leggendolo per intero prima di scrivere codice nuovo: nessuna lacuna da colmare, nessuna modifica necessaria. Rieseguito insieme al nuovo file Feature per regressione (vedi sotto).
- `Modules/User/app/Filament/Widgets/Profile/SuperAdminWidget.php`, `Modules/User/app/Providers/Filament/AdminPanelProvider.php` — **non toccati**, come da Owned File/Module Scope ("non provider, non widget class"). **Nota rilevante per lo swarm**: durante questa sessione `git status`/`git diff` hanno mostrato che entrambi i file avevano nel working tree modifiche non mie — `SuperAdminWidget.php` solo il drift innocuo pre-esistente (riga vuota dopo `declare(strict_types=1)`), ma `AdminPanelProvider.php` risultava **riportato a una versione precedente** (hook con stringhe letterali `'panels::auth.login.form.after'`/`'panels::user-menu.before'` invece delle costanti `PanelsRenderHook::*`, import di `SocialLoginWidget`/`TeamChangeWidget` assenti, blocchi di codice morto commentato reintrodotti) rispetto alla versione pulita letta a inizio sessione e rispetto a `HEAD`. Non l'ho toccato né ripristinato (stesso principio già applicato in 9.3: "mai revert di modifiche altrui senza motivo verificato"), ma non è stato incluso nel mio commit. Segnalato qui perché sembra un revert collaterale in corso da un altro agente dello swarm, pattern già documentato più volte in `memory/` per questo stesso epic.

**Verifiche AC (comandi reali, output riportato):**
1-3. AC#1-3 (visibilità super-admin/negate/nessuno): `Livewire::test(SuperAdminWidget::class)->assertSee(...)/assertDontSee(...)/assertSeeHtml('data-super-admin-state=...')`. Tutti e 3 PASS.
4. AC#4 (toggle inverte i ruoli): 2 test (super-admin→negate e viceversa) che assegnano il ruolo di partenza, chiamano `toggleSuperAdmin` via Livewire, e verificano `hasRole()` su `$user->fresh()`. PASS.
5. AC#5 (`isDiscovered()` false): coperto sia dal test Unit preesistente (riflessione sulla proprietà statica) sia da un nuovo test Feature che verifica l'assenza della classe da `Filament::getPanel('user::admin')->getWidgets()` (basato sulla lettura di `vendor/filament/filament/src/Panel/Concerns/HasComponents.php::discoverComponents()`, che salta esplicitamente le classi con `isDiscovered() === false`).
6. AC#6 (naming, niente `assertTrue(true)`): rispettato in entrambi i file.

**Bug/rischi reali scoperti e risolti scrivendo i test (non a priori):**
- **Cache store risolve su `database` in ambiente di test**: `.env.testing`/`phpunit.xml` impostano solo il legacy `CACHE_DRIVER=array`, ma Laravel 11+ legge `CACHE_STORE` (non impostato), quindi `config('cache.default')` risolve al default di `config/cache.php` = `'database'`. Spatie Permission (`permission.cache.store = 'default'`) tenta quindi di scrivere sulla tabella `cache` della connessione `mysql` (→ `quaeris_data_test`), che non esiste → `QueryException` a ogni `assignRole()`/`removeRole()`. **Confermato pre-esistente e non causato da questa story**: riprodotto lanciando per davvero (non a memoria) anche `Modules/User/tests/Feature/UserBusinessLogicTest.php` (già committato, 7 test falliti sullo stesso identico errore più altri difetti ambientali scorrelati: colonna `uuid` mancante su `teams`, tabella `media` mancante). Workaround applicato **solo nel mio file di test** (`config(['cache.default' => 'array']);` in `beforeEach`), senza toccare `TestCase.php`/`.env.testing` (fuori scope, file condivisi con altri agenti).
- **`XotData::make()` è un singleton su proprietà statica** (`Modules/Xot/app/Datas/XotData.php`, `private static ?self $instance`) che sopravvive fra i test dello stesso processo Pest; `getProfileModel()` mette in cache `$this->profile` alla prima chiamata, quindi un secondo test riceverebbe il Profile (e lo `user_id`/ruolo) del primo test. Verificato empiricamente: i test di visibilità fallivano solo nella suite completa, non isolati con `--filter`. Risolto **solo nel mio file di test** azzerando la proprietà statica via reflection in `beforeEach`, senza toccare `Modules/Xot` (fuori Owned File/Module Scope).
- **Setup ruoli realistico necessario**: creare in test solo il ruolo Spatie di partenza (es. solo `super-admin`) fa percorrere a `IsProfileTrait::toggleSuperAdmin()` il ramo di fallback `catch (RoleDoesNotExist $e)` (il ruolo opposto non esiste ancora), che si comporta diversamente dal ramo primario `assignRole()`/`removeRole()`. Corretto pre-creando **entrambi** i ruoli (`super-admin` e `negate-super-admin`, team_id null, guard web) prima di assegnare quello di partenza — replica lo scenario reale (ruoli seedati) e testa il percorso primario, quello rilevante per l'AC.
- **Collisione di sottostringa nei tooltip IT**: `tooltip.active` = "Super Admin" è una sottostringa letterale di `tooltip.negated` = "Nega Super Admin"; un `assertDontSee(tooltip.active)` nello stato negato falliva sempre. Marcatore distintivo: `data-super-admin-state` (non più `rotate-180`: i due stati sono due glifi, corona vs corona barrata).

**Quality gates (solo sul file nuovo, mai repo/modulo-wide — altri agenti in parallelo):**
- `php -l Modules/User/tests/Feature/Filament/Widgets/SuperAdminWidgetTest.php`: nessun errore di sintassi.
- `vendor/bin/phpstan analyse Modules/User/tests/Feature/Filament/Widgets/SuperAdminWidgetTest.php --memory-limit=-1`: **[OK] No errors** (dopo 2 fix: chiamata statica di un metodo d'istanza `TestCase::setupFilamentAdminPanel()` → `$this->setupFilamentAdminPanel()`; `assertNotNull()` ridondante su tipo già narrowed → `assertInstanceOf(User::class, ...)`).
- `bash tools/phpmd.sh Modules/User/tests/Feature/Filament/Widgets/SuperAdminWidgetTest.php text cleancode`: nessuna violazione (output vuoto, exit 0).
- phpinsights: non tentato, rimosso dal repo (incompatibile Pest 5), come da istruzione esplicita del task.
- `XDEBUG_MODE=coverage vendor/bin/pest Modules/User/tests/Feature/Filament/Widgets/SuperAdminWidgetTest.php Modules/User/tests/Unit/Filament/Widgets/SuperAdminWidgetTest.php`: **12 passed (29 assertions)**. Rieseguito anche con `--order-by=random` per escludere dipendenze dall'ordine (rilevanti dato il bug di stato singleton sopra): **12 passed** di nuovo, seed diverso.

**Rischio dati accettato consapevolmente (non introdotto da me, precedente già in codebase):** `Modules\Quaeris\Models\Profile` vive sulla connessione `quaeris` (`quaeris_data_test`, MySQL reale), che **non** è inclusa in `Modules/User/tests/TestCase.php::$connectionsToTransact = ['mysql', 'user']`. Le righe `profiles` create da `SuperAdminWidget::mount()` durante questi test **non vengono rollback automaticamente** e restano nel DB `*_test` reale. Non è una novità introdotta da questa story: `Modules/User/tests/Feature/UserBusinessLogicTest.php` (già committato) fa esattamente lo stesso. Non ho modificato `TestCase.php` per includere `quaeris` nelle transazioni: è un file condiviso, fuori Owned File/Module Scope, e la sua modifica impatterebbe tutti gli agenti paralleli sullo stesso repo.

**Nota multi-agente:** nessun'altra modifica concorrente ha toccato i due file di test durante la sessione (verificato via `git status` a inizio e fine lavoro). L'unica interferenza osservata è il revert collaterale in corso su `AdminPanelProvider.php` descritto sopra, non incluso nel mio commit.
