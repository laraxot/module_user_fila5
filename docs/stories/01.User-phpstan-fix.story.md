# BMAD Story - User PHPStan Fix
Date: Sun Sep  6 04:36:00 PM CEST 2026
Status: Done (2026-09-07, sessione pomeriggio)

## Aggiornamento 2026-09-07

Ripresa dello swarm PHPStan sul modulo `User` (foundation module,
`UserContract`/`XotData` invariati — nessuna firma pubblica toccata).

**Errori**: recon indicava ~19, verifica reale a cache pulita ne trovava 12,
poi 9 (2 erano falsi positivi da `tmpDir: /tmp/phpstan` condiviso fra sessioni
swarm concorrenti). **Esito finale: 9 -> 0.**

File toccati (7, tutti in `Modules/User`, nessuna modifica fuori modulo,
nessun tocco a `Modules/Xot`):
- `app/Filament/Resources/OauthAuthCodeResource.php`
- `app/Filament/Resources/OauthRefreshTokenResource.php`
- `app/Filament/Resources/TeamUserResource.php`
- `app/Filament/Resources/TenantUserResource.php`
- `app/Filament/Resources/TenantResource.php`
- `tests/Unit/UserExecuteCoverage50Test.php`
- `tests/Unit/UserModulePhpstanFixesTest.php`

Dettaglio pattern e razionale fix: vedi `Modules/User/docs/coverage.md`
(sezione "PHPStan (level max) — swarm fix 2026-09-07").

Nessun `mixed` introdotto. Nessuna necessita' di toccare `phpstan.neon`.

**Gate**: pint (scope-limitato, no `--dirty`) verde; phpmd scope-limitato ai
5 file sorgente 0 violazioni (il tool va in crash sull'intero modulo per un
file terzo non identificato, preesistente, non bloccante per questa story);
phpinsights non installato in questo repo (Pest 5 incompatibile, noto);
pest sui 2 test file toccati verde a runtime, suite completa del modulo non
attesa fino in fondo per carico host condiviso (altre sessioni swarm
concorrenti), fallimenti osservati nella porzione completata preesistenti e
non riconducibili a questi file.

**Concorrenza**: working tree del modulo aveva ~650 file gia'
modificati/non tracciati da altre sessioni attive in parallelo (probabile
`migration-foreignidfor-swarm`). Commit isolato ai 7 file di questa story via
`git add` esplicito, mai `-A`.

**Git sync**: commit locale creato su `dev`. Push a `laraxot` **bloccato** da
un problema gia' noto e documentato (non introdotto da questa sessione): il
repo del modulo e' un **shallow clone** (`.git/shallow` presente) e
`laraxot/dev` remoto e' divergente rispetto alla history locale tronca —
stesso sintomo gia' visto e non risolto su `Modules/Notify`
(second brain: `project_notify_shallow_clone_push_broken.md`) e gia'
documentato per `User` il giorno prima
(`docs/chat/2026-09-06-user-module-sync-blocked-concurrent-wip.md`).
`git merge-base`/`git status` (ahead/behind) falliscono con
"Could not read c7892391..."; `git fetch --unshallow` e' il rimedio noto ma
gia' provato-e-fallito su Notify con lo stesso errore lato remoto. Non e' un
problema risolvibile localmente: serve owner/re-clone pulito del modulo.
Il commit resta valido e sicuro in locale su `dev`, in attesa di sync.
<<<<<<< HEAD
<<<<<<< .merge_file_QHJLvt
=======

## Update 2026-09-21

Rilanciata scansione PHPStan max (`./vendor/bin/phpstan analyse Modules/User
--memory-limit=-1`) per verificare lo stato reale rispetto a una lista
attesa di ~17 errori (8 `return.missing`, 2 `missingType.generics`, 1
`method.abstract` su `TenantUserFactory`, 4 `array.duplicateKey` in
`lang/it/register_tenant.php`, 2 `pest.test.emptyClosure`). **Riscontro**:
tutti questi erano gia' stati risolti da una sessione precedente (WIP
concorrente non ancora committato in questo modulo, working tree con ~777
file modificati da un recupero di merge corrotto pre-esistente, HEAD
`51cbe0ae` — vedi nota sotto). Restavano solo **2 errori reali**:

1. `missingType.generics` — `app/Contracts/PassportHasApiTokensContract.php:51`,
   `createToken()` dichiarava `PersonalAccessTokenResult` (classe generica
   `@template TValue`) senza specificare `TValue`. Fix: ripristinato
   `@return PersonalAccessTokenResult<Token>` nel docblock, coerente con il
   contratto gemello `Modules/Xot/app/Contracts/PassportHasApiTokensContract.php`
   (stessa annotazione) e con la history git del file stesso (la riga era
   presente prima che una risoluzione di merge la eliminasse per errore).
2. `return.type` — `app/Http/Controllers/Socialite/RedirectToProviderController.php:32`,
   `__invoke()` dichiara `Illuminate\Http\RedirectResponse` ma
   `Socialite::driver($provider)->redirect()` e' tipizzato (via
   `Laravel\Socialite\Contracts\Provider::redirect()`) come
   `Symfony\Component\HttpFoundation\RedirectResponse|Illuminate\Http\RedirectResponse`.
   Fix: aggiunto un type-guard reale (`instanceof` + `throw new
   \RuntimeException(...)` se il tipo non e' quello Illuminate atteso),
   stesso pattern gia' presente in una versione precedente dello stesso
   file (vedi history pre-merge-conflict). Nessun `@var`/`assert()`/cast,
   nessun allargamento di tipo.

**File toccati (2)**:
- `app/Contracts/PassportHasApiTokensContract.php`
- `app/Http/Controllers/Socialite/RedirectToProviderController.php`

**Verifica**: `phpstan analyse Modules/User --memory-limit=-1` → 0 errori
(oltre ai bucket strutturali noti `typeCoverage.constantTypeCoverage` /
`method.internalClass` su `expect()`/`it()->with()`, fuori scope). `php -l`
su entrambi i file ok. Pint scope-limitato ai 2 file (mai `--dirty`):
`phpdoc_separation`/`phpdoc_align` sul contract, `unary_operator_spaces`/
`no_unused_imports`/`not_operator_with_successor_space` sul controller.

**Pest**: nessun test dedicato a questi 2 file (grep su `tests/` non trova
riferimenti diretti a `RedirectToProviderController` o
`PassportHasApiTokensContract`). Eseguita la suite Socialite piu' vicina
(`tests/Unit/Actions/Socialite` + `tests/Feature/Actions/Socialite`):
57 passed, 9 failed, 4 risky, 1 todo. I 9 fallimenti sono tutti in
`tests/Feature/Actions/Socialite/LoginUserActionTest.php` con
`ModelNotFoundException: No query results for model
[Modules\User\Models\User]` in `LoginUserAction.php:33`
(`$socialiteUser->user()->firstOrFail()`) — dati di test/DB mancanti, non
riconducibile ai 2 file toccati da questa story (nessuna relazione di
codice) e pre-esistente. Non lanciato alcun `migrate:fresh`/`refresh`.

**Concorrenza**: NON toccato nessuno degli altri ~775 file gia' modificati
nel working tree del modulo (recupero merge corrotto di un'altra sessione,
HEAD `51cbe0ae` "fix(user): recover 466 files corrupted by botched
laraxot/dev merge" — history ancora con marker di conflitto non risolti,
working tree gia' ripulito ma non committato). Commit di questa story
scoped via pathspec esplicito ai 2 soli file sopra.
>>>>>>> .merge_file_17BH5w
=======
>>>>>>> 350420cb (Check & fix styling)
