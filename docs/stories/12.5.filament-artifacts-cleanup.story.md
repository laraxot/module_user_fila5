---
id: "12.5"
epic: 12
story: "5"
title: "Igiene fs del modulo: no junk committato"
slug: filament-artifacts-cleanup
status: backlog
scope: module:User
epic_title: 12 — Audit UI/Filament modulo User
related:
  - ../bmad/filament-ux-brainstorming.md
  - ../bmad/filament-ux-architecture.md
  - 10.3.retire-auth-livewire-twins.story.md
  - 10.4.retire-gdpr-profile-livewire.story.md
---

# 12.5: Igiene fs del modulo: no junk committato

**Story ID:** 12.5  
**Epic:** 12 — Audit UI/Filament modulo User  
**Slug:** filament-artifacts-cleanup  
**Status:** backlog

## Story

Come **maintainer del repo**,
voglio **che il modulo non contenga dipendenze node, backup e file tooling/spazzatura dentro i namespace applicativi**,
così che **clone, scan di sicurezza e code search restino leggibili e la taglia del repo non cresca di decine di MB**.

## Evidenza (2026-09-22)

- `resources/views/node_modules/` — **92 MB** di vite/swiper/postcss/tailwind ecc. committati.
- `lang/it/` — **98 file** `*.backup_20260216_*` orfani (es. `actions.php.backup_20260216_092226`).
- `.php-cs-fixer.dist.php` dentro `app/Filament/Resources/` e `app/Filament/Clusters/Passport/Resources/OauthAuthCodeResource/Pages/` (config tool in namespace business).
- Artefatti di lavoro: `app/Filament/Resources/UserResource/RelationManagers/ModulesRelationManager.wip`,
  `app/Filament/Traits/{CanDeleteAccount,CanLogoutOtherBrowserSessions,HasCachedAction,HasHiddenAction,HasTwoFactorAuthentication}.test`,
  `app/Filament/Widgets/Auth/BaseAuthWidget.php.no`, `app/Filament/Widgets/Auth/links.md`,
  `app/Filament/Widgets/LogoutWidget.php.corrected`, `app/Models/Project.test`,
  `app/Listeners/Auth/SendEmailVerificationNotification.test`,
  `resources/views/pages/{about,pages/index}.blade.php.no`,
  `database/migrations/...create_user_authentications_table.wip`.

## Acceptance Criteria

1. `resources/views/node_modules/` assente (rimozione con `git rm -r`).
2. `lang/it/*.backup_20260216_*` assenti (98 file), nessun altro `*.backup*` aggiunto.
3. Nessun `.php-cs-fixer.dist.php` dentro `app/` (config stile solo alla root del repo/laravel).
4. File `.wip/.no/.test/.corrected` (**non** rinominati in test, già in cartella test o comunque non rinominati da questa story) rimossi; i `.test` già integrati come test Pest veri restano invariati.
5. `app/Http/Livewire/_components.json` (cache alias morti, vedi 10.3/10.4) rimosso solo quando la cartella `Http/Livewire` viene vuotata da 10.4 — **non anticipare**.
6. `git diff --check` pulito; il diff di questa story non tocca codice PHP applicativo vivo (zero modifiche semantiche).

## Tasks / Subtasks

- [ ] `git rm -r resources/views/node_modules` (AC: #1)
- [ ] `git rm lang/it/*.backup_*` (AC: #2)
- [ ] `git rm` dei `.php-cs-fixer.dist.php` e artefatti elencati (AC: #3, #4)
- [ ] Verifica `du -sh` taglia modulo e grep `backup_2026|node_modules` (AC: #1, #2)
- [ ] Coordinate con 10.4 per `_components.json` (AC: #5)

## Testing

- Nessun test funzionale (cleanup). Scansione: `du -sh`, `find ... -name '*.backup*' -o -name 'node_modules' -o -name '*.wip' -o -name '*.no'` → zero hit nei path elencati.
- `composer go`/`php artisan view:clear` OK per confermare che nessuna vista punta a `views/node_modules`.

## Dependency Maps

- **Blocked by:** 10.4 solo per l'item `_components.json` (AC #5); il resto è indipendente.
- **Blocks:** 12.1–12.4 no; riduce rumore per code review dei fix.
- **External:** nessuno.

## Owned File/Module Scope

- `resources/views/node_modules/` — delete
- `lang/it/*.backup_20260216_*` — delete
- `app/Filament/**` artefatti (`.wip/.no/.test/.corrected`, `.php-cs-fixer.dist.php`) — delete
- `app/Http/Livewire/_components.json` — ritiro **con** 10.4 (AC #5)
- **Locked:** nessun file PHP applicativo vivo viene toccato