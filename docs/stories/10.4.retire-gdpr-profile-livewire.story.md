---
id: "10.4"
epic: 10
story: "4"
title: "DeleteAccount widget + ritiro Privacy/Terms User"
slug: retire-gdpr-profile-livewire
status: blocked
scope: module:User
epic_title: 10 — Http/Livewire restante → widget
github:
  repository: https://github.com/laraxot/module_user_fila5
  issues: https://github.com/laraxot/module_user_fila5/issues/100
  discussions: https://github.com/laraxot/module_user_fila5/discussions/101
  issue_status: "aperto #100 — 2026-09-21"
related:
  - ../bmad/livewire-widget-prd.md
  - ../bmad/livewire-widget-tech-spec.md
  - ../bmad/livewire-inventory.md
  - ../architecture/user-gdpr-decoupling.md
---

# 10.4: DeleteAccount Filament + Privacy/Terms fuori User

**Story ID:** 10.4  
**Epic:** 10 — Http/Livewire restante → widget  
**Slug:** retire-gdpr-profile-livewire  
**Status:** blocked (vedi Dev Agent Record — 2026-09-21)

> **Non implementare ora.** `DeleteUserAction` locked.

## GitHub (tracciamento)

| Risorsa | Ruolo |
|---------|-------|
| [laraxot/module_user_fila5#100](https://github.com/laraxot/module_user_fila5/issues/100) | Campagna padre |
| [discussions/101](https://github.com/laraxot/module_user_fila5/discussions/101) | Architettura widget-only |

## Story

Come **utente del profilo**,
voglio **cancellare l’account da UI Filament e non vedere testi legali in Livewire User**,
così che **Gdpr possieda Privacy/Terms e User non ospiti `dddx`**.

## Acceptance Criteria

1. UI delete account è widget o sezione pagina Filament profilo; chiama `DeleteUserAction`. Security: password confirm, solo utente corrente, nessuna cancellazione senza `success` dell’action. [Source: livewire-widget-prd.md#FR-C006]
2. Testi da lang `user::`, zero toast hardcoded italiani. [Source: livewire-widget-prd.md#FR-C006]
3. `Http/Livewire/Profile/DeleteAccount.php` e vista Livewire assenti. [Source: livewire-widget-architecture.md#adr-c001]
4. `PrivacyPolicy` e `TermsOfService` assenti da User. Hook terms nel provider resta commentato (non riattivare). [Source: livewire-widget-prd.md#FR-C007] [Source: livewire-widget-architecture.md#adr-c006]
5. Grep User: zero `dddx('wip')` in UI. [Source: livewire-inventory.md]
6. `AdminPanelProvider` non cambia in 10.4 (memoria Gdpr/Notify già commentata). [Source: livewire-widget-architecture.md#adr-c004]
7. Cartella `app/Http/Livewire` vuota a fine 10.4 (dopo 9.3, 10.1–10.3). [Source: livewire-widget-prd.md success]

## Tasks / Subtasks

- [ ] Widget/page delete account + lang (AC: #1, #2)
- [ ] Delete DeleteAccount HTTP (AC: #3)
- [ ] Delete PrivacyPolicy + TermsOfService User (AC: #4, #5)
- [ ] Grep Http/Livewire vuoto (AC: #7)
- [ ] Nessun diff provider (AC: #6)

## Testing

- Delete: password sbagliata non cancella; password ok + action success → redirect `/`.
- Grep: zero classi HTTP Livewire.

## Dependency Maps

- **Blocked by:** 9.3 (SuperAdmin HTTP già via), 10.1–10.3 (altri HTTP via) per AC #7; DeleteAccount UI può partire in parallelo sui **propri** file
- **Blocks:** chiusura issue #100
- **External:** modulo Gdpr per le pagine legal

## Owned File/Module Scope

- `app/Filament/Widgets/Profile/DeleteAccountWidget.php` (o pagina profilo esistente) — nuovo/adattato
- `app/Http/Livewire/Profile/DeleteAccount.php` — delete
- `app/Http/Livewire/PrivacyPolicy.php` — delete
- `app/Http/Livewire/TermsOfService.php` — delete
- viste `livewire/profile/delete-account`, `livewire/privacy-policy`, `livewire/terms-of-service` — delete
- **Locked:** `DeleteUserAction`, `AdminPanelProvider`
- **Non toccare PHP Gdpr** in questa story salvo conferma che le pagine legal già esistono; se mancano, issue sul repo Gdpr, non reimplementare in User

## Dev Agent Record

**2026-09-21 — subagent-10.4 — status: blocked, zero file di implementazione toccati**

Letta la story per intero prima di scrivere codice (come richiesto). Non ho implementato
nulla oltre a questo aggiornamento di frontmatter/Dev Agent Record, per i motivi verificati
sotto — coerenti con la nota già presente nella story stessa ("Non implementare ora.
`DeleteUserAction` locked.") e con l'istruzione ricevuta di fermarmi su un conflitto reale
invece di improvvisare fuori scope.

Evidenze raccolte (comandi reali, non a memoria):

1. **Lock multi-agente attivo sulla directory owned da questa story.**
   `Modules/User/app/Filament/Widgets/Profile.lock` (verificato con
   `bashscripts/lock/check.sh`) risulta:
   ```
   LOCKED at 2026-09-21T15:55:37+02:00
   Task: superadmin-widget-9.1
   Agent: 093815d6
   ```
   Età al momento della verifica: ~137s — lock vivo, non stale (soglia stale = 60 min per
   `bashscripts/lock/docs/README.md`). Questo lock copre esattamente
   `app/Filament/Widgets/Profile/`, la directory in cui l'"Owned File/Module Scope" di
   questa story richiede di creare/adattare `DeleteAccountWidget.php`. Il lock è intestato
   a un'altra story (9.1, SuperAdmin widget) e a un altro agente, non a 10.4.

2. **Il file target esiste già, non tracciato da git, con un bug reale.**
   `Modules/User/app/Filament/Widgets/Profile/DeleteAccountWidget.php` (git status: `??`,
   quindi scritto da un'altra sessione ancora in corso, non da me) esiste e nel metodo
   `destroy()` chiama:
   ```php
   app(DeleteUserAction::class)->run($user, $this->delete_confirm_password);
   ```
   Ho letto sia `Modules/User/app/Actions/User/DeleteUserAction.php` sia il trait
   `Spatie\QueueableAction\QueueableAction` che usa
   (`vendor/spatie/laravel-queueable-action/src/QueueableAction.php`): nessuno dei due
   definisce un metodo `run()`. `DeleteUserAction` espone solo `execute(User $user, string
   $confirmPassword): array{success: bool, message: string}` (come infatti la vecchia
   `Http/Livewire/Profile/DeleteAccount.php` già chiama correttamente). Il widget esistente
   quindi non funziona — coerente con l'avviso "DeleteUserAction locked" già scritto in
   testa alla story.

3. **Conclusione.** Editare o sostituire `DeleteAccountWidget.php` ora significherebbe
   scrivere dentro una directory il cui lock è intestato a un altro agente/task in questo
   stesso momento (swarm in corso, richiesto esplicitamente "in parallelo" dall'utente),
   rischiando di sovrascrivere lavoro altrui in corso. La nota di story preesistente
   ("Non implementare ora") risulta quindi verificata come reale, non solo dichiarata.

**File toccati in questa sessione:** solo questo file
(`Modules/User/docs/stories/10.4.retire-gdpr-profile-livewire.story.md`, frontmatter
`status` + questa sezione). Nessun file applicativo modificato o cancellato.

**Non fatto (in attesa di sblocco):**
- Widget/pagina delete account (AC #1, #2) — bloccato dal lock su
  `app/Filament/Widgets/Profile/` (punto 1) e dal bug verificato nel file già presente
  (punto 2), da correggere prima di poter procedere.
- Delete `Http/Livewire/Profile/DeleteAccount.php` + vista (AC #3) — non cancellato per non
  rimuovere l'unica implementazione funzionante di `destroy()` prima che un widget
  funzionante la sostituisca.
- Delete `PrivacyPolicy`/`TermsOfService` Http/Livewire + viste (AC #4, #5) — non toccati:
  la nota "Non implementare ora" in testa alla story non distingue tra le sue parti, e
  nessuna verifica indipendente è stata fatta sull'esistenza delle pagine legal in Gdpr
  (richiesta esplicita della story prima di procedere qui).
- Grep Http/Livewire vuoto (AC #7) — dipende dai punti sopra.
- `AdminPanelProvider` — non toccato, come da vincolo esplicito ("Locked") e da istruzione
  ricevuta; risulta peraltro anch'esso sotto lock attivo di un altro agente/task
  (`notify-fqcn-12.1`) nello stesso istante.

**Check eseguiti:** nessun phpstan/phpmd/Pest lanciato — zero file applicativi modificati
in questa sessione, non c'è nulla su cui i check avrebbero valore oltre a quanto già
verificato sopra (lettura sorgente di `DeleteUserAction` e del trait `QueueableAction`).

**Prossimo passo per chi riprende la story:** attendere il rilascio del lock su
`app/Filament/Widgets/Profile.lock` (o coordinarsi con l'agente/task `superadmin-widget-9.1`
che lo detiene), poi correggere `DeleteAccountWidget.php` (`->run()` → `->execute()`,
gestendo l'array `{success, message}` come fa già la versione Livewire), verificare
l'esistenza delle pagine legal in Gdpr, e solo allora eseguire in ordine le cancellazioni
previste da AC #3–#7.
