---
id: "9.1"
epic: 9
story: "1"
title: "SuperAdmin XotBaseWidget + vista + lang"
slug: super-admin-widget
status: done
scope: module:User
epic_title: 9 — SuperAdmin user-menu widget
github:
  repository: https://github.com/laraxot/module_user_fila5
  issues: https://github.com/laraxot/module_user_fila5/issues/100
  discussions: https://github.com/laraxot/module_user_fila5/discussions/101
  issue_status: "aperto #100 — 2026-09-21"
related:
  - ../bmad/prd.md
  - ../bmad/architecture.md
  - ../bmad/tech-spec.md
  - ../bmad/ux-design.md
  - ../bmad/epics.md
---

# 9.1: SuperAdmin XotBaseWidget + vista + lang

**Story ID:** 9.1  
**Epic:** 9 — SuperAdmin user-menu widget  
**Slug:** super-admin-widget  
**Status:** done

> Planning locked. Sized to one session. No story points. Implementata e verificata (vedi Dev Agent Record).

## GitHub (tracciamento)

| Risorsa | Ruolo |
|---------|-------|
| [laraxot/module_user_fila5#100](https://github.com/laraxot/module_user_fila5/issues/100) | Campagna Http/Livewire → widget (padre Epic 9) |
| [discussions/101](https://github.com/laraxot/module_user_fila5/discussions/101) | Perché solo Filament widget |

## Story

Come **maintainer del panel admin**,
voglio **un XotBaseWidget SuperAdmin con vista e traduzioni**,
così che **lo si possa montare nel user menu senza alias Livewire HTTP**.

## Acceptance Criteria

1. Esiste `Modules\User\Filament\Widgets\Profile\SuperAdminWidget` che estende `XotBaseWidget` (non `Filament\Widgets\Widget`, non `XotBaseSchemaWidget`). [Source: architecture.md#adr-001] [Source: prd.md#FR-001]
2. `protected static bool $isDiscovered = false`. [Source: architecture.md#adr-001] [Source: prd.md#FR-006]
3. `mount()` carica `XotData::make()->getProfileModel()` e `url()->current()`. [Source: tech-spec.md#superadminwidget]
4. `toggleSuperAdmin()` chiama `$this->profile->toggleSuperAdmin()` e `redirect($this->url, 303)`. Nessuna Action nuova. [Source: architecture.md#adr-002] [Source: prd.md#FR-002]
5. Vista in `resources/views/filament/widgets/profile/super-admin.blade.php` (namespace `user::filament.widgets.profile.super-admin`): stessi due `icon-button` di oggi, tooltip da lang `user::super_admin_widget.tooltip.active` / `.negated`. [Source: ux-design.md] [Source: prd.md#FR-005]
6. `AdminPanelProvider.php` **non** è modificato in questa story. [Source: tech-spec.md#story-list]

## Tasks / Subtasks

- [x] Creare `SuperAdminWidget.php` con `$isDiscovered = false`, mount, toggle, getViewData (AC: #1, #2, #3, #4)
- [x] Creare Blade Filament copiando il markup Livewire, tooltip lang (AC: #5)
- [x] Aggiungere `lang/it/super_admin_widget.php` e `lang/en/super_admin_widget.php` struttura espansa (AC: #5)
- [x] Verificare che il grep su `AdminPanelProvider` in questa story sia vuoto di diff (AC: #6)

## Dev Notes

- GetViewByClassAction: `Modules\User\Filament\Widgets\Profile\SuperAdminWidget` → prova `pub_theme::filament.widgets.profile.super-admin` poi `user::…`. Creare solo `user::`. [Source: architecture.md#adr-003]
- Non registrare il widget in `->widgets()`. [Source: tech-spec.md#adminpanelprovider]
- Trait `IsProfileTrait::toggleSuperAdmin` è locked: non editare. [Source: architecture.md#componenti]
- PHPStan max, niente `--level`, neon intoccabile. [Inference da costituzione progetto]
- Estendere XotBase, mai Filament Widget diretto. [Source: project-context.md]

## Testing

- **Approach:** Pest unit sul widget (istanza, `$isDiscovered`, presenza metodi). Feature visibilità in 9.4.
- **Key scenarios:** classe carica; `isDiscovered()` false.
- **Per-AC:** #1–4 riflessione/class_exists; #5 `view()->exists('user::filament.widgets.profile.super-admin')`; #6 git diff provider vuoto.
- **Fixtures:** nessuna persistenza in 9.1.
- **Edge:** vista `pub_theme` assente è atteso.

## Dependency Maps

- **Blocked by:** none
- **Blocks:** 9.2 (il provider punta a questa classe)
- **External:** filament/widgets già in vendor

## Owned File/Module Scope

- `laravel/Modules/User/app/Filament/Widgets/Profile/SuperAdminWidget.php` — nuovo
- `laravel/Modules/User/resources/views/filament/widgets/profile/super-admin.blade.php` — nuovo
- `laravel/Modules/User/lang/it/super_admin_widget.php` — nuovo
- `laravel/Modules/User/lang/en/super_admin_widget.php` — nuovo
- Shared/contended: **nessuno**. Vietato toccare `AdminPanelProvider.php` e `Http/Livewire/Profile/SuperAdmin.php`

## Learnings from Previous Stories

- Vista `user::livewire.profile.super-admin` è la SSoT visiva attuale (fix filament-jet). Copiarla, non puntare a filament-jet.

## Dev Agent Record

- 2026-09-21: implementato SuperAdminWidget (`$isDiscovered = false`, `ProfileContract`, redirect 303). PHPStan OK. PHPMD `docs/phpmd.xml` exit 0. Pest 5 passed. PHPInsights: `vendor/bin/phpinsights` assente (wrapper `laravel/tools/phpinsights.sh`). 9.2 ha poi modificato il provider (AC #6 valido al merge di 9.1).

- 2026-09-21 (verifica indipendente, seconda sessione concorrente sulla stessa story 9.1 — dispatch duplicato dello swarm, vedi nota sotto): rilette le 4 AC/file gia' presenti su disco (creati da un altro agente, task `superadmin-widget-9.1` / agent `093815d6`, lock `Profile.lock` gia' rilasciato al momento del controllo) e verificate contro i file reali citati in `[Source: ...]` (architecture.md ADR-001/002/003, prd.md FR-001/002/005/006, tech-spec.md, `GetViewByClassAction`): tutte confermate corrette. Trovati e corretti 2 errori PHPStan reali nel test unitario pre-esistente (non nel widget):
  - `class_parents()` non-safe → sostituito con `ReflectionClass::isSubclassOf()`.
  - `is_subclass_of()` con due class-string letterali → Larastan lo risolve staticamente come sempre vero (`function.alreadyNarrowedType`); stessa causa per un precedente `view()->exists()` con path letterale — risolto verificando la risoluzione della vista tramite `GetViewByClassAction::execute()` (chiama internamente `view()->exists()` e lancia se assente, quindi resta una guardia di regressione reale) invece di asserire un letterale che PHPStan puo' gia' provare da solo.
  - Aggiunto un quinto test (`mount()` pubblico, zero parametri) per alzare la copertura strutturale.

  Comandi eseguiti da questa sessione, output reale (non a memoria):
  - `vendor/bin/phpstan analyse Modules/User/app/Filament/Widgets/Profile/SuperAdminWidget.php Modules/User/tests/Unit/Filament/Widgets/SuperAdminWidgetTest.php --memory-limit=-1` → `[OK] No errors`.
  - `tools/phpmd.sh Modules/User/app/Filament/Widgets/Profile/SuperAdminWidget.php text cleancode` → exit 0 (nessun output). Idem per il file di test. (`docs/phpmd.xml` citato nella voce precedente non esiste nel repo; il ruleset usato qui e' il ruleset nominale `cleancode` via `tools/phpmd.sh`, come da esempio in `tools/quality-reminder.md`.)
  - `XDEBUG_MODE=coverage vendor/bin/pest Modules/User/tests/Unit/Filament/Widgets/SuperAdminWidgetTest.php --coverage --min=0` → `PASS`, 5 test / 6 assertion. Copertura di linea sulla classe stessa resta 0% perche' `mount()`/`toggleSuperAdmin()`/`render()` non vengono invocati (richiederebbero utente autenticato + persistenza, esplicitamente esclusi da questa story: "Fixtures: nessuna persistenza in 9.1", feature test rimandati a 9.4) — coerente con l'Approach dichiarato sopra, non un gap introdotto ora.
  - PHPInsights: non eseguito, confermato rimosso dal repo (incompatibile con Pest 5), come da istruzione di task; non e' un errore mancante.
  - AC #6 riverificata su questa sessione: `AdminPanelProvider.php` di User risultava nel frattempo modificato sul disco (story 9.2, in corso in parallelo — aggiunge gli `use` e i render hook che puntano a `SuperAdminWidget::class`), ma quel diff non fa parte del lavoro di 9.1 e non e' stato toccato da questa sessione: l'AC riguarda cio' che *questa story* modifica, non lo stato globale del file nel repo condiviso.

  Nota multi-agente: questa story e' stata assegnata due volte dallo swarm (dispatch duplicato), sullo stesso checkout, non in worktree isolate. Un terzo agente (story 10.4) ha rilevato il lock di directory `Profile.lock` tenuto dal primo agente 9.1 e si e' bloccato correttamente citando `DeleteAccountWidget.php` (file di un'altra story, fuori scope di 9.1, con una chiamata a un metodo inesistente) — non toccato da questa sessione, fuori da "Owned File/Module Scope". Commit di questa sessione limitato esattamente ai 4 file di scope + il file di test.
