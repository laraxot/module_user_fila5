---
id: "9.2"
epic: 9
story: "2"
title: "AdminPanelProvider: hook SuperAdmin sul widget"
slug: admin-panel-provider-hook
status: done
scope: module:User
epic_title: 9 — SuperAdmin user-menu widget
github:
  repository: https://github.com/laraxot/module_user_fila5
  issues: https://github.com/laraxot/module_user_fila5/issues
  discussions: https://github.com/laraxot/module_user_fila5/discussions
  issue_status: "da aprire — gh non autenticato il 2026-09-21"
related:
  - ../bmad/tech-spec.md
  - ../bmad/architecture.md
  - ../bmad/prd.md
  - ./9.1.super-admin-widget.story.md
---

# 9.2: AdminPanelProvider — hook SuperAdmin sul widget

**Story ID:** 9.2  
**Epic:** 9 — SuperAdmin user-menu widget  
**Slug:** admin-panel-provider-hook  
**Status:** done

> Unico file PHP di questa story: `AdminPanelProvider.php`. **Non implementare ora.**

## GitHub (tracciamento)

| Risorsa | Ruolo |
|---------|-------|
| DA CREARE | issue `laraxot/module_user_fila5` |
| DA CREARE | discussion |

```bash
gh -R laraxot/module_user_fila5 issue create --title "9.2 AdminPanelProvider hook SuperAdminWidget" --body "Canon: laravel/Modules/User/docs/stories/9.2.admin-panel-provider-hook.story.md"
```

## Story

Come **operatore del panel**,
voglio **che `/admin` monti il widget SuperAdmin al posto del Livewire `profile.super-admin`**,
così che **il user menu non dipenda più da quell’alias**.

## Acceptance Criteria

1. Il `registerRenderHook` del SuperAdmin (oggi `Blade::render("@livewire('profile.super-admin')")`) monta `SuperAdminWidget::class` via `@livewire('…FQCN…')`. [Source: tech-spec.md#adminpanelprovider] [Source: prd.md#FR-003]
2. Lo hook usa `Filament\View\PanelsRenderHook::USER_MENU_BEFORE` (costante), non solo la stringa magica, **per il blocco SuperAdmin**. [Source: tech-spec.md#adminpanelprovider]
3. L’hook `team.change` (`Blade::render("@livewire('team.change')")`) è **byte-invariato** in questa story. [Source: architecture.md#adr-004]
4. Non si decommenta `View::make('user::badges.super-admin')`. [Source: tech-spec.md#adminpanelprovider] [Source: prd.md#FR-002]
5. Nessun `->widgets([SuperAdminWidget::class])`, nessun `userMenuItems` nuovo, nessun `pages()` per questo toggle. [Source: tech-spec.md#adminpanelprovider] [Source: prd.md#FR-006]
6. `parent::panel($panel)` resta la prima trasformazione del panel. [Source: tech-spec.md#adminpanelprovider]

## Tasks / Subtasks

- [x] Aggiungere `use` di `PanelsRenderHook` e `SuperAdminWidget` (AC: #1, #2)
- [x] Sostituire **solo** il blocco hook SuperAdmin (circa righe 50–54) (AC: #1, #2)
- [x] Diff: zero cambi sul blocco `team.change` e sui commenti Gdpr/Notify (AC: #3)
- [x] Non toccare il badge commentato se non per lasciarlo commentato (AC: #4)
- [x] Non aggiungere widget alla discovery/lista panel (AC: #5, #6)

## Spec del blocco (contratto)

**Prima:**

```php
FilamentView::registerRenderHook(
    'panels::user-menu.before',
    static fn (): string => Blade::render("@livewire('profile.super-admin')"),
);
```

**Dopo (da implementare in 9.2, non in questa sessione docs):**

```php
FilamentView::registerRenderHook(
    PanelsRenderHook::USER_MENU_BEFORE,
    static fn (): string => Blade::render(
        '@livewire(\'' . SuperAdminWidget::class . '\')',
    ),
);
```

Il Livewire HTTP può restare su disco fino a 9.3: dopo 9.2 semplicemente non è più montato.

## Dev Notes

- File reale oggi: `laravel/Modules/User/app/Providers/Filament/AdminPanelProvider.php`. [Source: architecture.md#componenti]
- Filament 5 risolve già `USER_MENU_BEFORE` nel compiled `user-menu` (`FilamentView::renderHook(PanelsRenderHook::USER_MENU_BEFORE)`). [Inference da vendor compile]
- `Blade::render('@livewire(FQCN)')` è lo stesso meccanismo usato per `team.change` e Notify: non inventare `HtmlString` / `new SuperAdminWidget`. [Source: tech-spec.md]
- Se 9.1 non è mergiata, 9.2 500 per class not found: rispettare Blocked-by. [Source: epics.md]

## Testing

- **Approach:** smoke autenticato `/admin` (200, non InvalidArgumentException). Dettaglio toggle in 9.4.
- **Per-AC:** #1 HTML contiene l’icona per super-admin; #3 sorgente provider contiene ancora `team.change`; #5 dashboard page non elenca SuperAdmin come widget card.
- **Edge:** utente senza i due ruoli: menu senza icona corona, nessun 500.

## Dependency Maps

- **Blocked by:** 9.1 — la classe widget deve esistere
- **Blocks:** 9.3 (rimozione Livewire), 9.4 (test sul panel)
- **External:** Filament ViewManager render hooks

## Owned File/Module Scope

- `laravel/Modules/User/app/Providers/Filament/AdminPanelProvider.php` — **unico file PHP**
- Shared/contended: questo file. Nessun’altra story dell’epic lo elenca. Serializzare se un altro agente tocca gli hook Socialite/team.

## Learnings from Previous Stories

- Il 500 `filament-jet` partiva da questo hook: non reintrodurre namespace Jet. (da 9.1 / fix precedente)

## Dev Agent Record

- 2026-09-21: implementato il blocco hook SuperAdmin in `AdminPanelProvider.php` (`use PanelsRenderHook;`, `use SuperAdminWidget;`, `registerRenderHook(PanelsRenderHook::USER_MENU_BEFORE, ... @livewire(SuperAdminWidget::class) ...)`). Tutte le 6 AC riverificate a mano contro il blob committato (`git show HEAD:...`), non a memoria: #1/#2 righe 53/55, #3 riga 49 (`team.change` byte-invariato), #4 riga 54 (badge ancora commentato), #5 nessuna `widgets()`/`userMenuItems()`/`pages()` attiva, #6 riga 25 (`parent::panel($panel)` prima istruzione). Citazioni `[Source: ...]` verificate contro i file reali: `tech-spec.md` righe 65-100 (blocco "AdminPanelProvider — modifiche"), `prd.md` FR-002/003/006, `architecture.md` ADR-004 + sezione Componenti — tutte corrispondenti.

**Race multi-agente reale, non teorica — gestita con commit chirurgico:** all'arrivo il file `AdminPanelProvider.php` era gia' sotto lock attivo e fresco (`AdminPanelProvider.php.lock`, `LOCKED at 2026-09-21T16:03:46+02:00`, `Task: user-9.3-10.1-10.2`, `Agent: cursor-grok-parent`, eta' ~4 min al primo controllo — sotto la soglia di staleness di 60 min di `bashscripts/lock/README.md`, quindi non rubabile). Ho aspettato (poll ogni 20-30s, ~7 minuti) invece di forzare, come da `README.md` ("Don't force unlock: if another agent is modifying, wait") e da questa stessa story ("Serializzare se un altro agente tocca gli hook Socialite/team"). Il lock si e' liberato alle 16:10:32. In quella finestra l'altro agente aveva gia' convertito **anche** gli hook `team.change` (story 10.1) e login OAuth (story 10.2) allo stesso `@livewire(FQCN)` pattern, e rimosso i blocchi morti commentati (`pages()`/`userMenuItems()`/tenant) — lavoro legittimo ma di un'altra story, non ancora committato, e **incompatibile con l'AC #3 di questa story se preso alla lettera** (richiede `team.change` byte-invariato, ma nel frattempo un'altra story lo ha convertito). Non ho ne' accettato ne' distrutto quel lavoro: invece di un commit sull'intero file (che avrebbe incluso il diff di 10.1/10.2, fuori scope, mai verificato da questa sessione) ho costruito il commit di 9.2 chirurgicamente — `git hash-object` sul contenuto target (HEAD + solo il blocco SuperAdmin), un indice temporaneo separato (`GIT_INDEX_FILE=...`) seminato da `git read-tree HEAD`, `git write-tree` + `git commit-tree -p HEAD`, poi `git update-ref` con compare-and-swap sul vecchio HEAD — senza mai toccare l'indice reale condiviso ne' il working tree degli altri agenti. Verificato con `git diff-tree HEAD <newtree>` che il nuovo commit cambia **un solo file**. Il working tree e' stato riportato esattamente allo stato lasciato dall'altro agente (md5 identico prima/dopo) cosi' che 10.1/10.2/9.3 possano completare il loro commit separatamente.

Trovato inoltre, gia' presente nell'indice condiviso **prima** di qualunque mia azione: 20 file staged (`git diff --cached --stat`) di cancellazione dei Livewire twin `Auth/*` (Login, Register, Logout, Passwords/*, Verify + blade + test) — scope della story 10.3 "retire-auth-livewire-twins", non mio. Non li ho toccati (il mio `commit-tree` ha usato un indice temporaneo separato, non quello reale) e restano staged per chi possiede 10.3.

Il file story stesso e' stato sovrascritto da un'altra sessione concorrente mentre lavoravo (letto la prima volta con `status: done`, `github.issues` #100 gia' aperta e Dev Agent Record compilato con una nota su un test browser MCP fallito; ritrovato poco dopo, prima di questo aggiornamento, resettato a `status: ready-for-dev`, Dev Agent Record vuoto, sezione GitHub "DA CREARE — gh non autenticato"). Ho preso quest'ultima versione su disco come base (verificata con `check.sh`/lock prima di scrivere) e non ho tentato di ricreare o inventare i numeri issue/discussion: la nota "gh non autenticato" e' un fatto tecnico verificabile, la precedente #100/#101 non lo era in questa sessione. Non ho eseguito alcun comando `gh` (creazione issue = pubblicazione pubblica, richiede autorizzazione esplicita dell'utente che questa sessione headless non puo' raccogliere).

Comandi eseguiti, output reale (non a memoria), contro il contenuto esatto poi committato (verificato byte-identico con `diff`):
  - `php -l Modules/User/app/Providers/Filament/AdminPanelProvider.php` → `No syntax errors detected`.
  - `vendor/bin/phpstan analyse Modules/User/app/Providers/Filament/AdminPanelProvider.php --memory-limit=-1` → `[OK] No errors`.
  - `tools/phpmd.sh Modules/User/app/Providers/Filament/AdminPanelProvider.php text cleancode` → 6 warning `StaticAccess` (facade Laravel/Filament, idiomatico in questo repo). Confrontato riga per riga con lo stesso comando sul contenuto di HEAD pre-9.2: stesso numero, stessa categoria, stesse facade (`FilamentView`, `Blade`) — **zero nuove violazioni introdotte da questa story**, pre-esistenti e fuori scope da correggere qui.
  - Nessun nuovo test Pest: fuori "Owned File/Module Scope" di questa story (unico file PHP dichiarato: `AdminPanelProvider.php`), test sul panel esplicitamente rimandati a 9.4 in Dependency Maps.

Commit: `a7675952a3196e47f69076383aa2e078de1bdd58` su `dev` (parent `2c83c20c874db7d9ab6b809215bb90d31304b670`), 1 file, +4/-2. Lock su `AdminPanelProvider.php` e su questo file story rilasciati a fine sessione.

**Ripristino post-collaterale (stesso giorno):** il commit `edda1dc5a` (story 10.3 "retire-auth-livewire-twins", altro agente, altro scope: `Http/Livewire/Auth/**`) ha inavvertitamente riportato *questo file* allo stato `ready-for-dev`/Dev Agent Record vuoto — collaterale quasi certo di un indice git condiviso stale (lo stesso indice dove avevo gia' notato, prima del mio commit, 20 file staged di story 10.3 non miei: l'agente ha evidentemente committato quell'indice senza prima aggiornarlo su questo path). Rilevato confrontando `git rev-parse HEAD:<path>` (conteneva ancora `status: ready-for-dev`) con il working tree su disco (conteneva questa versione `done`, mai persa perche' mai sovrascritta localmente). Nessuna azione su `edda1dc5a` o sui suoi 23 file: ripristinato **solo** questo path con un commit dedicato, stesso metodo chirurgico (`read-tree HEAD` corrente + `update-index --cacheinfo` sul solo path + `write-tree` + `commit-tree -p HEAD` + `update-ref` con compare-and-swap), verificato con `git diff-tree` che cambia un solo file rispetto al nuovo HEAD.

**Seconda revert collaterale (stesso giorno, ~2 min dopo il primo ripristino):** il commit `3e9dfd869` ("fix(job): retire dead Broad Livewire component", story 12.1, ancora un altro agente/modulo) ha ripetuto lo stesso pattern: ha ripristinato questo file a `status: ready-for-dev`/Dev Agent Record vuoto pur non nominandolo nel proprio messaggio di commit ne' nel proprio scope dichiarato. Due agenti distinti (story 10.3 e story 12.1), su due moduli diversi, hanno entrambi fatto collateral-revert dello stesso path in meno di 3 minuti: non e' un incidente isolato ma un **bug sistemico dello swarm** — piu' agenti condividono lo stesso working tree/indice git e periodicamente eseguono un commit "ampio" (verosimilmente `git commit -a` o equivalente) che include qualunque cosa il *loro* indice locale considerava ancora modificato, cioe' lo stato di questo file **prima** del mio primo commit (`a7675952a`/`1a12f9df7`), mai riallineato nel loro indice. Ripristinato di nuovo con lo stesso metodo chirurgico a singolo path. Segnalato in `memory/multi-agent-shared-index-collateral-revert.md` per lo swarm: non e' risolvibile rincorrendo altri restore, servirebbe che ogni agente committi solo i path che possiede esplicitamente (mai `-a`/`-A`) o che questo path venga escluso dai commit ampi finche' lo swarm e' attivo.
