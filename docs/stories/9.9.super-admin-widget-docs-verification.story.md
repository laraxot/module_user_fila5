---
id: "9.9"
epic: 9
story: "9"
title: "SuperAdmin widget — verifica docs campagna + diagnosi icone residua"
slug: super-admin-widget-docs-verification
status: done
scope: module:User
epic_title: 9 — SuperAdmin user-menu widget
github:
  repository: https://github.com/laraxot/module_user_fila5
  issues: https://github.com/laraxot/module_user_fila5/issues/100
  issue_status: "aperto #100 — gh autenticato 2026-09-21"
related:
  - ./9.1.super-admin-widget.story.md
  - ./9.7.super-admin-widget-icons-final.story.md
  - ../bmad/livewire-inventory.md
  - ../bmad/livewire-widget-admin-panel-provider.md
  - ../bmad/advantages-filament-only.md
---

# 9.9: verifica documentazione campagna + diagnosi residua icone

**Richiesta utente** (2026-09-21): documentare (non implementare) la conversione
`Http/Livewire/Profile/SuperAdmin.php` → Filament widget, le modifiche a
`AdminPanelProvider`, l'inventario dei componenti Livewire convertibili, i
vantaggi widget-only; poi "migliora SuperAdminWidget che le icone non si
vedono" + "sistema tutte le segnalazioni phpstan".

## Esito — parte documentale

**Tutta la documentazione richiesta esisteva gia' ed e' stata riverificata
contro il disco** (nessun file riscritto, solo verifica):

| Richiesta | Documento canonico | Stato |
|-----------|--------------------|-------|
| Conversione SuperAdmin → widget | stories 9.1→9.4 + `bmad/tech-spec.md` | ✅ done, verificato |
| Modifiche `AdminPanelProvider` | `bmad/livewire-widget-admin-panel-provider.md` | ✅ mappa 3 hook FQCN, verificata su file (rr. 25-38) |
| Inventario componenti convertibili | `bmad/livewire-inventory.md` (piattaforma, per-modulo) | ✅ coerente con disco: User residui = `DeleteAccount`, `TermsOfService`, `PrivacyPolicy`, `Livewire/Logout` (Cluster C → 10.4) |
| Vantaggi + urgenza widget-only | `bmad/advantages-filament-only.md` + `Xot/docs/bmad/livewire-widget-advantages.md` | ✅ approvato |

`SuperAdmin.php` HTTP non esiste piu' su disco (eliminato in commit `63d5346`);
il provider monta solo FQCN widget. `_components.json` resta stale (14 alias
morti) — residuo gia' tracciato in inventory → story 10.4.

## Esito — PHPStan

`php -d memory_limit=2G ./vendor/bin/phpstan analyse` (fleet `Modules/`,
cache `/tmp/phpstan` svuotata prima): **[OK] No errors** — zero segnalazioni,
baseline 0 confermata. Nessuna correzione necessaria in questa sessione.

## Esito — icone SuperAdminWidget

Stato su disco (convergenza post-race documentata in 9.7):
`x-filament::icon-button` + `icon="user-superman"` / `icon="user-clark-kent"`,
SVG in `resources/svg/` auto-registrati da `XotBaseServiceProvider::registerBladeIcons()`
(prefix `user` → `user-superman` risolve `superman.svg`).

Verifiche eseguite in questa sessione (tinker, app reale):
- `svg('user-superman')` → `BladeUI\Icons\Svg`, markup completo e non vuoto.
- `Blade::render('<x-filament::icon-button icon="user-superman" ...>')` →
  `<svg class="fi-icon fi-size-md">` inline corretto.
- CSS compilato `public_html/css/filament/filament/app.css`: regole `.fi-icon`,
  `.fi-icon.fi-size-sm`, `.fi-icon-btn` presenti → icona dimensionata e colorata.

**Root cause residua individuata** (non ancora corretta — file sotto lock di
sessione concorrente `cursor-grok`, task `user-9.8-icons`, lock 18:00):
la vista renderizza l'icona SOLO se `isSuperAdmin()` OR `isNegateSuperAdmin()`.
Un utente con NESSUNO dei due ruoli ottiene `<span>` vuota → widget
"invisibile". Se la segnalazione "icone non si vedono" arriva da un utente
senza quei ruoli, il fix e' un ramo `@else` (es. clark-kent `color="gray"`,
tooltip `tooltip.inactive`) in `super-admin.blade.php` — da coordinare con la
sessione che detiene i lock, non applicato qui per non violarli.

## Lock / coordinamento

- Lock attivi altrui (NON toccati): `super-admin.blade.php`,
  `SuperAdminWidget.php`, `superman.svg`, `clark-kent.svg`,
  `SuperAdminWidgetTest.php`, `bmad/epics.md` — task `user-9.8-icons`.
- Lock stale rimosso: `docs/sprint-status.yaml.lock` (2026-09-16, 5 giorni).

## Dev Agent Record

- Solo verifica + documentazione; zero edit a codice.
- Aggiornato `docs/sprint-status.yaml` (entry `User/9.9-*`).
- Memoria second brain: `superadmin-widget-icons-verification-2026-09-21.md`.
