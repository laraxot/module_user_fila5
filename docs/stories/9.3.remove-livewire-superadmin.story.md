---
id: "9.3"
epic: 9
story: "3"
title: "Rimuovere Livewire HTTP SuperAdmin"
slug: remove-livewire-superadmin
status: done
scope: module:User
epic_title: 9 — SuperAdmin user-menu widget
github:
  repository: https://github.com/laraxot/module_user_fila5
  issues: https://github.com/laraxot/module_user_fila5/issues
  discussions: https://github.com/laraxot/module_user_fila5/discussions
  issue_status: "da aprire — gh non autenticato il 2026-09-21"
related:
  - ../bmad/prd.md
  - ./9.2.admin-panel-provider-hook.story.md
---

# 9.3: Rimuovere Livewire HTTP SuperAdmin

**Story ID:** 9.3  
**Epic:** 9 — SuperAdmin user-menu widget  
**Slug:** remove-livewire-superadmin  
**Status:** done

## GitHub (tracciamento)

| Risorsa | Ruolo |
|---------|-------|
| DA CREARE | issue `laraxot/module_user_fila5` |
| DA CREARE | discussion |

```bash
gh -R laraxot/module_user_fila5 issue create --title "9.3 remove Http Livewire SuperAdmin" --body "Canon: laravel/Modules/User/docs/stories/9.3.remove-livewire-superadmin.story.md"
```

## Story

Come **maintainer User**,
voglio **una sola UI SuperAdmin**,
così che **l’alias `profile.super-admin` non possa più 500-are il panel**.

## Acceptance Criteria

1. File `app/Http/Livewire/Profile/SuperAdmin.php` assente. [Source: prd.md#FR-004]
2. File `resources/views/livewire/profile/super-admin.blade.php` assente (SSoT è la vista Filament di 9.1). [Source: prd.md#FR-004]
3. Grep modulo User: zero `profile.super-admin`, zero `Http\Livewire\Profile\SuperAdmin`, zero `filament-jet::livewire.profile.super-admin`. [Source: tech-spec.md#livewire-da-ritirare]
4. `AdminPanelProvider` **non** viene rimaneggiato in 9.3 (già switchato in 9.2). [Source: epics.md sequenza]
5. Badge `resources/views/badges/super-admin.blade.php` si può lasciare (non è montato). Non è richiesto eliminarlo. [Inference]

## Tasks / Subtasks

- [x] Eliminare la classe Livewire (AC: #1) — file già assente in working tree, deletion staged nel commit di questa story
- [x] Eliminare la vista Livewire (AC: #2)
- [x] Grep e pulire riferimenti docs solo se puntano al path morto come “codice vigente” — i docs BMAD di questo epic restano (AC: #3) — 1 riga non-BMAD corretta in `filament_errors.md`
- [x] Non aprire `AdminPanelProvider.php` (AC: #4)

## Dev Notes

- Ordine: se 9.3 precede 9.2, `/admin` 500. Blocked-by è vincolante. [Source: decision-log.md] [Source: epics.md]
- Non toccare `Team\Change` né altri Livewire HTTP. [Source: prd.md out of scope]
- `user::badges.super-admin` è l’alternativa statica scartata: lasciarla non rimonificata. [Source: brainstorming.md]

## Testing

- **Approach:** grep CI-like in 9.4; qui basta phpstan sul tree User senza class orphan.
- **Per-AC:** #1–3 file/grep; #4 git diff provider vuoto rispetto a 9.2.
- **Edge:** cache view compilate: `view:clear` dopo delete.

## Dependency Maps

- **Blocked by:** 9.2
- **Blocks:** none (9.4 può parallelizzare sui test)
- **External:** none

## Owned File/Module Scope

- `laravel/Modules/User/app/Http/Livewire/Profile/SuperAdmin.php` — delete
- `laravel/Modules/User/resources/views/livewire/profile/super-admin.blade.php` — delete
- Shared/contended: **non** `AdminPanelProvider.php`

## Learnings from Previous Stories

- Dopo 9.2 l’alias non è più referenziato: la delete è sicura.

## Dev Agent Record

**Status finale:** done. Tutti e 5 gli AC verificati con prove reali (comandi eseguiti, non a memoria).

**File toccati (Owned File/Module Scope):**
- `Modules/User/app/Http/Livewire/Profile/SuperAdmin.php` — delete (AC#1). Trovato già cancellato nella working tree ma mai staged/committato (drift preesistente di una sessione precedente); `git add` per includerlo in questo commit.
- `Modules/User/resources/views/livewire/profile/super-admin.blade.php` — delete (AC#2), tramite `git rm -f` (la working tree aveva un diff di whitespace preesistente irrilevante alla cancellazione).
- `Modules/User/docs/filament_errors.md` — 1 frase corretta (AC#3, task esplicito "pulire riferimenti docs solo se puntano al path morto come codice vigente"). La frase "Oggi il provider monta ancora `@livewire('profile.super-admin')`" era falsa dopo 9.2; sostituita con lo stato reale (widget). Non è un doc `docs/bmad/` dell'epic, quindi non protetto dalla clausola "i docs BMAD restano".
- `AdminPanelProvider.php` — **non toccato**, come da AC#4 (confermato via `git status`: nessuna modifica mia staged su questo file; era in modifica concorrente di un altro agente dello swarm nello stesso momento, notificato dall'harness — ignorato correttamente).

**Verifiche AC (comandi reali, output riportato):**
1. AC#1: `test -f .../SuperAdmin.php` → assente. Pest `RetiredChromeLivewireTest.php::"super admin http component is gone"` → PASS (class_exists=false, file assente).
2. AC#2: `test -f .../super-admin.blade.php` (livewire) → assente dopo `git rm -f`.
3. AC#3: grep mirato (non substring generico, per evitare falsi positivi con `user::filament.widgets.profile.super-admin` del widget 9.2) per `'profile.super-admin'` come alias Livewire, `@livewire(...super-admin...)`, `Http\Livewire\Profile\SuperAdmin`, `filament-jet::livewire.profile.super-admin`: zero hit in codice vigente. Unici hit residui: `RetiredChromeLivewireTest.php` (test che verifica intenzionalmente l'assenza, non "codice vigente"), la story 9.3 stessa (auto-riferimento), e i doc `docs/bmad/**` (protetti esplicitamente dal task).
4. AC#4: `git status`/`git diff --cached` → `AdminPanelProvider.php` non compare tra i file staged da me.
5. AC#5: badge `resources/views/badges/super-admin.blade.php` lasciato presente, non montato — nessuna azione richiesta.

**Quality gates (solo sui file toccati, mai repo/modulo-wide — altri agenti in parallelo):**
- `php -l` su `RetiredChromeLivewireTest.php` (impattato dalla cancellazione, non modificato da me): nessun errore di sintassi.
- `vendor/bin/phpstan analyse Modules/User/tests/Unit/Http/Livewire/RetiredChromeLivewireTest.php --memory-limit=-1`: **[OK] No errors**.
- `bash tools/phpmd.sh Modules/User/tests/Unit/Http/Livewire/RetiredChromeLivewireTest.php text cleancode`: nessuna violazione.
- I due file "toccati" veri e propri sono entrambi delete (nessun `.php` residuo da passare a phpstan/phpmd) più 1 doc Markdown (non applicabile a phpstan/phpmd). phpinsights non è nel repo (rimosso, incompatibile Pest 5) — non tentato, come da istruzione.
- `XDEBUG_MODE=coverage vendor/bin/pest Modules/User/tests/Unit/Http/Livewire/RetiredChromeLivewireTest.php`: **3 passed (8 assertions)**.
- `php artisan view:clear`: "Compiled views cleared successfully" (edge case cache view, da Testing/Edge della story).

**Nessun nuovo test Pest scritto.** La sezione Testing della story stesso dice esplicitamente: "Approach: grep CI-like in 9.4; qui basta phpstan sul tree User senza class orphan" e "Per-AC: #1–3 file/grep". La story non richiede nuovi test per 9.3 (deferiti a 9.4); l'unico test esistente pertinente (`RetiredChromeLivewireTest.php`, di proprietà della story 10.3) già copre e verifica AC#1 e passa. Non l'ho modificato per non uscire dall'Owned File/Module Scope e per non toccare un file di proprietà di un'altra story in un repo con swarm multi-agente attivo in parallelo.

**Deviazione dalle istruzioni generiche del task (punto 3, convenzioni widget XotBaseWidget/RegisterWidget.php):** non applicabile. Story 9.3 non crea nessun widget nuovo — la conversione Livewire→widget è già stata fatta ed è già committata in 9.2 (`SuperAdminWidget.php` esiste, estende già le convenzioni del modulo, ed è già agganciato in `AdminPanelProvider.php`). 9.3 è **solo** rimozione del codice Livewire ormai morto. Verificato leggendo la story per intero prima di scrivere codice, come richiesto.

**Nota su race multi-agente:** durante il lavoro, l'harness ha notificato una modifica concorrente a `AdminPanelProvider.php` (diff di whitespace, namespace) da un altro agente dello swarm. Non l'ho toccata né revertita, coerente con AC#4 e con la policy "mai revert di modifiche altrui senza motivo verificato".
